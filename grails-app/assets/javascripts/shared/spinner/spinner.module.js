/**
 *  @module spinner
 *  @memberof shared
 *  @see module:shared
 *  @see module:app
 *  @description
 *  Exposes `Spinner` directive
 */
(function (projectName, angular) {

  'use strict';

  angular
    .module('app.shared.spinner', []);

})(window.projectName = window.projectName || {}, angular);
